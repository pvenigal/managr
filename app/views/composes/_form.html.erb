<%= form_for(@compose) do |f| %>
  <% if @compose.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@compose.errors.count, "error") %> prohibited this compose from being saved:</h2>

      <ul>
      <% @compose.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div class="login-page">
  <div class="form">
    <form class="login-form">
    <%= f.label "From" %><br>
    <%= f.text_field :name %>
    <%= f.label "reply to email" %><br>
    <%= f.text_field :email %>
    <%= f.label :subject %><br>
    <%= f.text_field :subject %>

    <%= f.label :message %><br>
    <%= f.text_area :message %>
        
<br>
<div class="actions">
    <%= f.submit "Send" %>
        </div>
      </form>
    </div>
</div>
<% end %>
